import { ElementRef, OnInit, EventEmitter } from '@angular/core';
export declare class DaumAddressComponent implements OnInit {
    result: EventEmitter<Object>;
    options: any;
    styleClass: String;
    private el;
    private debug;
    constructor(el: ElementRef);
    ngOnInit(): void;
    private print(msg);
    private daumApiCallback(data);
    openDaumApi(): boolean;
    private loadDaumApi();
}
