import { DoCheck, ElementRef, Renderer2, SimpleChanges } from '@angular/core';
import { OnChanges, OnDestroy, OnInit } from '@angular/core';
import { ColumnInfoService } from '../common/column-info.service';
import { IdService } from '../common/id.service';
import { CellContext } from '../rendering/common/cell-context';
import { FocusGroup } from './focus-group';
import { LogicalCell } from './logical-cell.interface';
import { NavigationService } from './navigation.service';
/**
 * @hidden
 */
export declare class LogicalCellDirective implements LogicalCell, OnInit, OnChanges, OnDestroy, DoCheck {
    focusGroup: FocusGroup;
    private element;
    private columnInfoService;
    private idService;
    private navigationService;
    private renderer;
    private cellContext;
    logicalColIndex: number;
    logicalRowIndex: number;
    logicalSlaveCell: boolean;
    colSpan: number;
    rowSpan: number;
    groupItem: any;
    readonly id: string;
    readonly ariaColIndex: number;
    private navigationChange;
    constructor(focusGroup: FocusGroup, element: ElementRef, columnInfoService: ColumnInfoService, idService: IdService, navigationService: NavigationService, renderer: Renderer2, cellContext: CellContext);
    ngOnInit(): void;
    ngDoCheck(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private onNavigationChange(e);
    private updateElement();
    private register();
    private isFocusable();
    private isFocused();
}
