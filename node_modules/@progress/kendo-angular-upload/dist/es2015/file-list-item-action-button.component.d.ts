import { ElementRef } from '@angular/core';
import { FileInfo } from './file-info';
import { UploadService } from './upload.service';
import { AsyncSettings } from './async-settings';
import { LocalizationService } from '@progress/kendo-angular-l10n';
/**
 * @hidden
 */
export declare class FileListItemActionButtonComponent {
    private uploadService;
    private localization;
    file: FileInfo;
    async: AsyncSettings;
    disabled: boolean;
    progress: Number;
    retryButton: ElementRef;
    actionButton: ElementRef;
    private retryFocused;
    private actionFocused;
    constructor(uploadService: UploadService, localization: LocalizationService);
    setButtonClasses(type: string): Object;
    setActionButtonClasses(file: FileInfo): Object;
    setActionButtonTitle(file: FileInfo): string;
    setRetryButtonTitle(): string;
    onFocus(type: string): void;
    onBlur(type: string): void;
    onClick(uid: string, type?: string, _event?: any): void;
    isVisible(file: FileInfo, type?: string): boolean;
}
