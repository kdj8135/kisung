import { ElementRef, EventEmitter, OnDestroy, Renderer2 } from '@angular/core';
/**
 * Represents the Kendo UI TextArea directive for the Inputs components for Angular.
 * Used to add floating label to any `textarea` element.
 *
 * @example
 * ```ts-no-run
 * <textarea kendoTextArea></textarea>
 * ```
 */
export declare class TextAreaDirective implements OnDestroy {
    private renderer;
    private textAreaElement;
    readonly elementClass: boolean;
    /**
     * @hidden
     */
    direction: string;
    /**
     * Fires each time the user focuses the `textarea` element.
     */
    onFocus: EventEmitter<any>;
    /**
     * Fires each time the `textarea` element gets blurred.
     */
    onBlur: EventEmitter<any>;
    /**
     * Gets the value of the `textarea` element.
     */
    readonly value: string;
    id: string;
    private listeners;
    constructor(renderer: Renderer2, textAreaElement: ElementRef, rtl: boolean);
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
}
